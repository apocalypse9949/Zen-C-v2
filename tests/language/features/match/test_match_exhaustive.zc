// test: compile-fail
// expected_errors: Match is not exhaustive. Missing cases: None

enum Option {
    Some(int),
    None
}

pub fn main() {
    let opt = Option::Some(42);

    match opt {
        Some(x) => {}
    }
}
